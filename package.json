{
  "name": "dify-openai-chat-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Aurora PostgreSQL-backed OpenAI Chat Completions reverse proxy for Vercel.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "migrate": "tsx scripts/migrate.ts",
    "test": "tsx --test tests/*.test.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@aws-sdk/rds-signer": "^3.1100.0",
    "@vercel/functions": "^3.7.6",
    "@vercel/node": "^5.9.3",
    "ipaddr.js": "^2.2.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
