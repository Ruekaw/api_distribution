run = "npm start"
build = "npm run build"
entrypoint = "src/server.ts"

[[ports]]
localPort = 3000
externalPort = 80

[deployment]
run = ["npm", "start"]
deploymentTarget = "cloudrun"
ignorePorts = false
